import * as constants from '../../code/main/constants.js';

//
// generate a model
//

export default class GenModelBaseClass
{
    constructor(view)
    {
        this.view=view;
    }
    
    generate(name,sizeFactor,inDebug)
    {
        return(null);
    }
    
}
