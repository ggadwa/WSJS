- big
    - remove all static
    - remove all class fields (ugh)
    - webGL instead of webGL2

- rerun docs

- networking
    - entities need boxes passed on join
    - need animation running or timing information for animation to work
    - need to transmit show/hide meshes (as bitmask?)
    - need to synchronize map movements (movements might need their own clock)
    - predictions
    - need open system for passing message (like firing weapons, etc)

        this.currentAnimationIdx=-1;
        this.currentAnimationStartTimestamp=0;
        this.currentAnimationLoopStartTick=0;
        this.currentAnimationLoopEndTick=0;
        this.currentAnimationData=new Float32Array(4);
        
        this.queuedAnimationStop=false;
        
        this.queuedAnimationIdx=-1;
        this.queuedAnimationStartTimestamp=0;
        this.queuedAnimationLoopStartTick=0;
        this.queuedAnimationLoopEndTick=0;

- generator
    - better specs (generated with color?)
    - nodes don't seem right in certain rooms (like boxes)
    - the random color scheme

    - lights
       - regular
       - color changing
       - chasing (4 that go on and off)

    - bitmaps
       - pipe, needs random line & random screw line

    - decorations
       - don't repeat decorations
       - pool decorations
       - moving blocks (half size of segmentSize, in random circular motion)
       - better uv for pillars
       - control panel decorations
       - connectors for pipes

- demo
    - implement bot skill
    - mouse wheel broken chrome/windows
    - fix swimming

- reaper
    - random node start

- kart

- js docs (especially in the project_ classes)
    - doc main classes
    - doc project class
    - [got to wsjs directory] jsdoc -c "docs_config.json"

- slowly transition to public/private fields (especially in the project_ classes)
- SAFARI - wait for public/private class fields
- FIREFOX - wait for static class fields
- finish touch interface when iOS updated

