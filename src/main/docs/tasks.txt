SCRIPT REDO


---> kart
      - get rid of trackZOffset
      - move in new perp based AI
      - possibly move behind other kart if have bowling ball and certain distance

- developer turn off ambients like dialogs do

- karts
- player (need to preserve data changes)
- bot
- monsters

- EntityCacheClass gone!

- animations and sounds as new AnimationDef() and new SoundDef()
- sequences need scripts
- remove actions

  FINALLY: map_entity_list.addFromMap/addDynamic
- bots: need to recheck everything
   - make sure pauses at elevators are working

- ambients (on effects)
   - crackling fire
   - fountain
   - wind for castle
   - spooky mixed sounds for dungeon (add to wsjsMap)

- player: fall damage lights up all damage hit sides
- kart: make grid of places to go, always go to next closest one +1
- kart: crashes have weight effect

[NEW KART MAP - get rid of ground and put flag ground segments, fix seams]

- if no pose, just set ONCE


- rework multiplayer
   - logon needs to have project name, pass back game & map name
   - clients need to be in a paused state until final synch message
   - don't enter other players until synch is OK
   - call sync at very end (move stuff in stageNetworkConnectOK)

INTERFACE
  - menu by keys
  - gamepad support

DEMOS
  - kart: weird bug with laps near goal line
  - kart: going backwards
  - fps: pulses need to be in json
  - fps: bots run away if no ammo?
  - fps: death messages by game with json setup

MISC
  - run some more profiling
  - eventually clean up entity and unnecessary pass through APIs from old version

NETWORKING
  - need map_sync message (timestamp + map movements - round robin out to first player) todo partially implemented
  - predictions
  - need open system for passing message (like firing weapons, etc)
