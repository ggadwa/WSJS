export default class ProjectMapClass
{
    constructor(view,map)
    {
        this.view=view;
        this.map=map;
    }
    
        //
        // override to load and setup the map
        //
        
    load(callback)
    {
    }
}
